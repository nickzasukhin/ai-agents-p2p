apiVersion: v1
kind: ConfigMap
metadata:
  name: agent-config
  labels:
    app: agent-social-network
data:
  LOG_LEVEL: "info"
  LLM_PROVIDER: "openai"
  OPENAI_MODEL: "gpt-4o-mini"
  DISCOVERY_INTERVAL: "30"
