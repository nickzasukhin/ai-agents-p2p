apiVersion: v1
kind: Service
metadata:
  name: agent-service
  labels:
    app: agent-social-network
spec:
  type: ClusterIP
  selector:
    app: agent-social-network
  ports:
    - name: http
      port: 9000
      targetPort: 9000
      protocol: TCP
