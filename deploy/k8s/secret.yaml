# Placeholder — create the real secret with:
#   kubectl create secret generic agent-secrets \
#     --from-literal=OPENAI_API_KEY=sk-...
apiVersion: v1
kind: Secret
metadata:
  name: agent-secrets
  labels:
    app: agent-social-network
type: Opaque
data: {}
